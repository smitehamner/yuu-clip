{
  "name": "yuu-clip-frontend-build",
  "version": "0.0.0",
  "private": true,
  "description": "Dev-only frontend build toolchain (esbuild) for yuu-clip's web UI. Not shipped - the committed static/bundle.esm.js is the artifact; Node/esbuild are only needed to rebuild it. Run via `yuu-dev bundle`.",
  "scripts": {
    "build:esm": "node scripts/build-esm.mjs"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  }
}
