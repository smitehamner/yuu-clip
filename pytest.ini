[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -m "not live_remote"
markers =
    live_remote: opt-in test that calls the real Anthropic API (needs YUU_REMOTE_AI=1 + ANTHROPIC_API_KEY). Excluded from default runs; select with `pytest -m live_remote`.
