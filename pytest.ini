[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    golden: opt-in real-models path (real Whisper + real local LLM); excluded from every
        default run and skips unless its env-var inputs and runtime are present. Run via
        `yuu-dev test-golden` / scripts/test-golden.ps1.
